[package]
name = "koinon"
description = "κοινόν — typed application-bootstrap for forkwright Rust crates: integrated CLI, config, and telemetry init"
version = "0.2.1"
edition = "2024"
rust-version = "1.85"
license = "LicenseRef-PolyForm-Noncommercial-1.0.0"
repository = "https://github.com/forkwright/koinon"
keywords = ["tracing", "config", "clap", "bootstrap", "scaffolding"]
categories = ["development-tools"]
readme = "README.md"

[features]
default = ["telemetry", "config", "cli", "bootstrap"]
telemetry = ["dep:tracing", "dep:tracing-subscriber"]
config = ["dep:figment", "dep:serde"]
cli = ["dep:clap", "telemetry"]
bootstrap = ["cli", "config"]

[dependencies]
# Tracing (feature: telemetry)
tracing = { version = "0.1", optional = true }

# Error handling (always on)
snafu = "0.9"

# Config loading (feature: config)
figment = { version = "0.10", features = ["toml", "env"], optional = true }

# CLI prelude (feature: cli)
clap = { version = "4", features = ["derive", "env"], optional = true }

# Tracing (feature: telemetry) - expanded form: inline table exceeds 80 columns
[dependencies.tracing-subscriber]
version = "0.3"
features = ["env-filter", "fmt", "json"]
optional = true

# Config loading (feature: config) - expanded form: inline table exceeds 80 columns
[dependencies.serde]
version = "1"
default-features = false
features = ["derive"]
optional = true

[dev-dependencies]
figment = { version = "0.10", features = ["toml", "env", "test"] }
tempfile = "3"

[lints.rust]
future_incompatible = { level = "warn", priority = -1 }
nonstandard_style = { level = "warn", priority = -1 }
rust_2018_idioms = { level = "warn", priority = -1 }
unsafe_code = "deny"

[lints.clippy]
pedantic = { level = "warn", priority = -1 }
missing_errors_doc = "warn"
missing_panics_doc = "warn"
module_name_repetitions = "allow"
must_use_candidate = "allow"
dbg_macro = "deny"
todo = "deny"
unimplemented = "deny"
unwrap_used = "deny"
expect_used = "deny"
